# Generated app conventions

Read `PROJECT_STRUCTURE.md` first — it is the contract this repo is built
around, and the QA gate asserts it. The Mobile Builder generates INTO the
directories it defines and may not invent new top-level directories.

Expo SDK 57. Check the versioned docs at
https://docs.expo.dev/versions/v57.0.0/ before using an API.
